import cBottomBar from '../c-bottom-bar'
import pAuthor from 'components/p-author'
import pStart from 'components/p-start'

/* 组件中使用到的其他组件 */

const components = {
  cBottomBar,
  pAuthor,
  pStart
}

/* vue 组件属性合成并导出 */

export default {
  components
}
